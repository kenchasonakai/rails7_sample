## rails new command
```
dc run --rm web rails new . -d=postgresql -j=esbuild -c=sass
```

## rails new option

### -d=postgresql
select postgres

### -j=esbuild
select esbuild

### -c=sass
select sass
